# SCOPE — NIMA Link · ADT-RAISE Batch 2 · Module 2

ร่าง 1 ก.ย. 2569 — แก้ได้ถึงสิ้นสัปดาห์ที่ 6 หลังจากนั้นเปลี่ยนแล้วต้องรื้องาน

---

## ส่วนที่เลือกมาทำ

โครงงานเต็มคือ **NIMA Link** — แอปเชื่อมร้านยา / คลินิก / โรงพยาบาล กับผู้แทนยา
Module 2 หยิบมาทำส่วนเดียวคือ **คำขอหายา (match request)** ซึ่งเป็นวงจรหลักของระบบ
ส่วนที่เหลือ (แคตตาล็อกเต็ม · OCR ใบส่งของ · ตรวจใบอนุญาต · แดชบอร์ด · VIP) อยู่ใน Backlog Sprint 2

## ประโยคเดียว

> ระบบของฉันเก็บ **คำขอหายา** ที่ **ร้านยา / คลินิก** สร้างขึ้น
> แต่ละคำขอเลือก **รายการยา** ได้ และมีสถานะ **รอผู้แทนรับ → รับแล้ว / ปฏิเสธ**
> โดย **ผู้แทนยา** เป็นคนกดเปลี่ยน

## ตารางขอบเขต

| | LeaveEasy (ในคาบ) | NIMA Link (ของผม) |
|---|---|---|
| โฟลเดอร์หลัก | `leaveRequests` | `requests` — คำขอหายา |
| โฟลเดอร์ประเภท | `leaveTypes` | `products` — รายการยาที่เลือกได้ |
| โฟลเดอร์ย่อย | `approvals` | `replies` — คำตอบของผู้แทนในคำขอนั้น |
| ช่องบอกว่าเป็นของใคร | `requesterId` · `requesterName` | `buyerId` · `buyerName` · `buyerChannel` |
| สถานะทั้งหมด | รอพิจารณา · อนุมัติ · ไม่อนุมัติ | รอผู้แทนรับ · รับแล้ว · ปฏิเสธ |
| คนที่สร้างรายการ | พนักงาน | ร้านยา / คลินิก |
| คนที่เปลี่ยนสถานะ | หัวหน้า | ผู้แทนยา |
| ช่องข้อความยาวที่ AI จะอ่าน | `reason` | `note` — ร้านพิมพ์เองว่าต้องการอะไร ปริมาณเท่าไหร่ ด่วนแค่ไหน |
| งานที่ AI ช่วย (สัปดาห์ที่ 8) | จัดประเภทการลาอัตโนมัติ | อ่าน `note` แล้วเดารายการยาที่ตรงที่สุด + ระดับความเร่งด่วน |

## เช็ก 6 อย่างที่ Module 2 ต้องใช้

| # | ต้องมี | NIMA Link มีไหม |
|---|---|---|
| 1 | รายการหลักที่เพิ่มเข้ามาเรื่อย ๆ | `requests` |
| 2 | ช่องบอกว่ารายการนี้เป็นของใคร | `buyerId` — และเป็นเส้นแดงข้อ 6 ของโปรเจกต์อยู่แล้ว |
| 3 | สถานะที่เปลี่ยนได้ | 3 สถานะ ผู้แทนเป็นคนกด |
| 4 | ประเภทที่เลือกจากรายการ (จดชื่อซ้ำ) | `products` → จด `productName` ซ้ำลงใน `requests` |
| 5 | โฟลเดอร์ย่อยผูกกับรายการนั้น | `replies` |
| 6 | ช่องข้อความยาวที่คนพิมพ์เอง | `note` |

ครบ 6 ข้อ

## จำนวนที่พอดี

- โฟลเดอร์หลัก 1 · โฟลเดอร์ประกอบ 2 (`users` · `products`) · โฟลเดอร์ย่อย 1
- หน้าจอ 3–4: รายการคำขอ · สร้างคำขอใหม่ · รายละเอียดคำขอ · (แคตตาล็อกของผู้แทน)
- บทบาท 2: ร้าน/คลินิก (ผู้สร้าง) · ผู้แทน (ผู้เปลี่ยนสถานะ)
- สถานะ 3

## ทำไมส่วนนี้เข้าทางกับสัปดาห์ที่ 8 พอดี

โปรเจกต์นี้มีกติกาบังคับตัวเองอยู่แล้ว 2 ข้อที่กลายเป็นโจทย์ security rules ตรง ๆ

1. **OTC / MC เป็น hard gate** — ร้านยาขายยาสามัญ (OTC) กับคลินิก/โรงพยาบาล (MC) เป็นคนละสระว่ายน้ำ
   ห้ามเห็นคำขอข้ามช่องทางกัน ต้องกันที่ชั้นข้อมูล ไม่ใช่ซ่อนใน UI
2. **PDPA — ผู้แทนห้ามเห็นชื่อ/ที่อยู่/รูปของร้าน จนกว่าจะกดรับคำขอ**
   จึงต้องแยกข้อมูลติดต่อออกไปอยู่ใน `replies` แล้วเขียนกฎคุมแยก

## ข้อที่ยังไม่ปิด

- สถานะที่ 4 ของจริงคือ **ตกลงราคา** (ระบบจบตรงนั้น ไม่มีการชำระเงินในแอปตามเส้นแดงข้อ 2)
  Module 2 ตัดออกไว้ก่อนเพื่อคุมให้เหลือ 3 สถานะตามคู่มือ → อยู่ใน Backlog Sprint 2
- ข้อมูลตัวอย่างจะใช้ **ชื่อร้านสมมติทั้งหมด** ไม่ใช่ชุด สปสช. 1,229 แห่งที่มีอยู่จริง
  เพราะ Firestore Test mode เปิดให้ใครก็อ่านได้ และเงื่อนไขการเผยแพร่ข้อมูลราชการชุดนั้นยังไม่ได้ตรวจ

## Backlog Sprint 2

ของเดิมอยู่ครบแล้วที่ เอกสาร backlog ภายในของโปรเจกต์
(12 Epic / 132 AC) — สัปดาห์ที่ 9 ค่อยสรุปเป็น `BACKLOG.md` จากไฟล์นั้น
