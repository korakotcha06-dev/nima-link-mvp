# ACL — ตารางสิทธิ์ NIMA Link

ระบบมี **2 บทบาท** เก็บอยู่ที่ `users/{uid}.role` — หน้าเว็บอ่าน role จากที่นี่ที่เดียว
ไม่มีการส่ง role มาจากฝั่งหน้าจอ

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| **ร้านยา / คลินิก** (`buyer`) — คนสร้างรายการ | สมัครและเข้าสู่ระบบ · สร้างคำขอหายา · ดูคำขอ**ของร้านตัวเอง**ทั้งหมด · ลบคำขอของตัวเอง (ยืนยันก่อนลบทุกครั้ง) · เห็นชื่อรายการยาเฉพาะช่องทางของตัวเอง | **เปลี่ยนสถานะคำขอไม่ได้ แม้เป็นคำขอของตัวเอง** (อนุมัติของตัวเองไม่ได้) · **ดูคำขอของร้านอื่นไม่ได้** · ดูคำขอข้ามช่องทาง OTC/MC ไม่ได้ · ลบคำขอของร้านอื่นไม่ได้ · แก้ไขรายการยาในระบบไม่ได้ · เปลี่ยน role ของตัวเองไม่ได้ |
| **ผู้แทนยา** (`rep`) — คนเปลี่ยนสถานะ | สมัครและเข้าสู่ระบบ · ดูคำขอ**ในช่องทางเดียวกับตัวเอง** · กดรับคำขอ · กดปฏิเสธคำขอ · เห็นชื่อและเขตของร้าน**หลังกดรับแล้วเท่านั้น** | **สร้างคำขอไม่ได้** · **ลบคำขอไม่ได้** · **เห็นชื่อร้านก่อนกดรับไม่ได้** (เห็นได้แค่เขต) · ดูคำขอข้ามช่องทาง OTC/MC ไม่ได้ · เปลี่ยนสถานะซ้ำหลังตอบไปแล้วไม่ได้ · แก้ไขช่องอื่นของคำขอไม่ได้ นอกจากช่อง `status` · เปลี่ยน role ของตัวเองไม่ได้ |

## ที่มาของกฎแต่ละข้อ

- **ร้านอนุมัติของตัวเองไม่ได้** — คำขอต้องมีคนอีกฝั่งรับ ถึงจะเรียกว่าจับคู่สำเร็จ ถ้าร้านกดเองได้ ตัวเลขในระบบจะไม่มีความหมาย
- **ร้านดูของร้านอื่นไม่ได้** — คำขอบอกว่าร้านไหนของขาดอะไรอยู่ เป็นข้อมูลการค้าที่คู่แข่งข้างถนนเดียวกันไม่ควรเห็น
- **ผู้แทนเห็นชื่อร้านหลังกดรับเท่านั้น** — PDPA by design ผู้แทนที่ไม่ได้จะรับงาน ไม่มีเหตุผลต้องรู้ว่าเป็นร้านไหน
- **กันข้าม OTC / MC** — ร้านขายยากับคลินิก/โรงพยาบาลอยู่คนละกฎหมายควบคุม ล็อกที่โปรไฟล์ตั้งแต่สมัคร ไม่ใช่ช่องติ๊ก
- **ผู้แทนแก้ได้แค่ช่อง `status`** — ราคา จำนวน และรายละเอียดเป็นคำพูดของร้าน ผู้แทนแก้แทนไม่ได้

## บังคับใช้ที่ไหน

| ชั้น | สัปดาห์ที่ 7 (ตอนนี้) | สัปดาห์ที่ 8 (ต้องทำต่อ) |
|---|---|---|
| ปุ่มและเมนูบนหน้าจอ | บังคับแล้ว — อ่าน role จาก `users/{uid}` | คงเดิม |
| การกรองข้อมูลตอน query | บังคับแล้วฝั่งหน้าเว็บ | ย้ายลงเป็น security rules |
| Firestore Security Rules | **มีแค่กฎเดียว: ต้องล็อกอินก่อน** | เขียนกฎรายคอลเลกชันให้ตรงตารางนี้ |

🔴 ตอนนี้กฎฝั่งฐานข้อมูลยังหลวม — ใครสมัครเข้ามาก็ยังยิง query ตรงอ่านของคนอื่นได้
**ห้ามใส่ข้อมูลจริงของคนอื่นจนกว่าจะทำสัปดาห์ที่ 8 เสร็จ**
