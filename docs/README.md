# docs

ใส่ภาพหน้าจอ **Firebase Console** ที่เห็นข้อมูลตัวอย่างในคอลเลกชัน `requests` อย่างน้อย 5 รายการ
ตั้งชื่อไฟล์ว่า `firestore-console.png`

วิธีถ่าย: Firebase Console → Firestore Database → เลือกคอลเลกชัน `requests` → ถ่ายภาพหน้าจอให้เห็นทั้ง 5 document
