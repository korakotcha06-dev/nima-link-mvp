# SPEC — NIMA Link · Module 2 (คำขอหายา)

สรุปจากของที่ทำเสร็จจริงใน repo ณ 20 ก.ย. 2569 · ที่มาของขอบเขต → [`SCOPE.md`](SCOPE.md) · ตารางสิทธิ์เต็ม → [`ACL.md`](ACL.md)
เอกสารนี้อธิบาย **ระบบที่มีอยู่ตอนนี้** ไม่ใช่สิ่งที่ตั้งใจจะทำ — ส่วนที่ยังไม่ได้ทำอยู่ในหัวข้อ "ไม่ทำใน Module นี้"

---

## 1. ระบบนี้ทำอะไร

> ร้านยา / คลินิก สร้าง **คำขอหายา** เลือกรายการยาจากแคตตาล็อก พิมพ์รายละเอียดเป็นภาษาคน
> แล้ว **ผู้แทนยา** ในช่องทางเดียวกันเห็นคำขอนั้น และเป็นคนกด **รับ** หรือ **ปฏิเสธ**
> ระบบจบตรงที่ตกลงกันได้ — ไม่มีการซื้อขายในแอป

### กฎ 3 ข้อที่บังคับทุกชั้น

| # | กฎ | บังคับที่ไหน |
|---|---|---|
| 1 | **OTC / MC เป็น hard gate** — ล็อกที่โปรไฟล์ตั้งแต่สมัคร ไม่ใช่ช่องติ๊กให้ผู้ใช้เปลี่ยนเอง | ฟิลเตอร์ query ทุกหน้า + เช็กซ้ำตอนเปิดรายละเอียด |
| 2 | **ผู้แทนไม่เห็นชื่อร้าน จนกว่าจะกดรับ** — เห็นได้แค่เขต (PDPA by design) | `index.html` · `detail.html` และไม่ส่งชื่อร้านเข้า prompt ของ AI |
| 3 | **role อ่านจาก `users/{uid}` เท่านั้น** — ไม่รับจาก query string หรือ localStorage | `app/auth.js` → `loadProfile()` |

---

## 2. บทบาทผู้ใช้

เก็บที่ `users/{uid}.role` · ค่าคงที่อยู่ใน `app/auth.js` → `ROLE`

| role | ใคร | สร้างคำขอ | เปลี่ยนสถานะ | ลบ | เห็นอะไร |
|---|---|---|---|---|---|
| `buyer` | ร้านยา / คลินิก / โรงพยาบาล | ✅ | ❌ (อนุมัติของตัวเองไม่ได้) | ✅ เฉพาะของตัวเอง ยืนยันก่อนลบ | คำขอของตัวเองทั้งหมด |
| `rep` | ผู้แทนยา | ❌ | ✅ ครั้งเดียว | ❌ | คำขอในช่องทางเดียวกับตัวเอง · ชื่อร้านเห็นหลังกดรับ |

ทั้งสองบทบาทมี **ช่องทาง** (`channel`) ติดตัวเสมอ: `OTC` (ร้านขายยา) หรือ `MC` (คลินิก / โรงพยาบาล) — ข้ามช่องทางกันไม่ได้ทุกกรณี

**ไม่มีบทบาท admin** — ข้อมูลตั้งต้นใส่ผ่าน `seed.html` ด้วยมือ

---

## 3. หน้าจอ

5 ไฟล์ · ทุกหน้าเป็น HTML + ES module ล้วน ไม่มีขั้นตอน build

### 3.1 `login.html` — สมัครใช้งาน / เข้าสู่ระบบ
เข้าได้ทุกคน · มี **3 โหมดในไฟล์เดียว**

| โหมด | เมื่อไหร่ | ช่องที่กรอก |
|---|---|---|
| `login` | ค่าเริ่มต้น | อีเมล · รหัสผ่าน |
| `register` | กดแท็บ "สมัครใช้งาน" | อีเมล · รหัสผ่าน · ชื่อ · role · channel · เขต |
| `complete` | มีบัญชี Auth แต่ไม่มีเอกสาร `users/{uid}` | ชื่อ · role · channel · เขต (ไม่ต้องกรอกอีเมล/รหัสผ่านซ้ำ) |

โหมด `complete` มีไว้กันทางตันจากบั๊กที่เคยเจอ: `onAuthStateChanged` ยิงทันทีที่สมัครสำเร็จ แล้วตัดหน้าไปก่อน `setDoc` เขียนโปรไฟล์เสร็จ — โค้ดกันด้วยตัวแปร `busy` และ `saveProfile()` ลองเขียนซ้ำ 3 ครั้งพร้อม refresh token

### 3.2 `index.html` — รายการคำขอ (หน้าแรกหลังล็อกอิน)
ต้องล็อกอิน · แสดงผลต่างกันตาม role

- **buyer** — `where("buyerId","==",uid)` เห็นเฉพาะของร้านตัวเอง · มีปุ่ม "+ สร้างคำขอใหม่"
- **rep** — `where("buyerChannel","==",channel)` เห็นทั้งช่องทาง · ไม่มีปุ่มสร้าง · ใบที่ยังไม่ `รับแล้ว` แสดงเป็น "ร้านในเขต … (ยังไม่เปิดเผยชื่อ)"

เรียงใหม่→เก่าฝั่งหน้าเว็บ เพราะ `where` + `orderBy` คนละช่องต้องสร้าง composite index
แต่ละการ์ดลิงก์ไป `detail.html?id=…`

### 3.3 `new.html` — สร้างคำขอใหม่
**buyer เท่านั้น** · rep เปิดได้แต่เจอข้อความปฏิเสธและฟอร์มถูกซ่อน

ช่องในฟอร์ม: รายการยา (dropdown กรองด้วย `channel` ของตัวเอง) · จำนวน · หน่วย · ความเร่งด่วน · รายละเอียด (textarea)
ปุ่ม **"ให้ AI ช่วยเลือกรายการยาจากรายละเอียด"** = ผู้ช่วย AI ระดับ 1 — เสนอค่าลงฟอร์ม คนกดส่งเอง
ส่งแล้วได้สถานะ `รอผู้แทนรับ` เสมอ แล้วเด้งกลับ `index.html`

### 3.4 `detail.html` — รายละเอียดคำขอ
ต้องล็อกอิน · เช็กสิทธิ์ซ้ำตอนโหลด — buyer ที่ไม่ใช่เจ้าของ และ rep ต่างช่องทาง เห็นข้อความปฏิเสธ

| ใคร | เห็น / ทำได้ |
|---|---|
| buyer เจ้าของ | ข้อมูลครบ · ปุ่มลบ (มีกล่องยืนยันในหน้า ไม่ใช่ `confirm()`) · ข้อความว่าเปลี่ยนสถานะเองไม่ได้ |
| rep ช่องทางเดียวกัน · สถานะ `รอผู้แทนรับ` | ชื่อร้านถูกปิดไว้ · ปุ่ม **รับคำขอ** / **ปฏิเสธ** · การ์ดผู้ช่วย AI ระดับ 2 |
| rep · ตอบไปแล้ว | ถ้า `รับแล้ว` เห็นชื่อร้าน · ปุ่มหายไป ("เปลี่ยนสถานะซ้ำไม่ได้") |

ปุ่มรับ/ปฏิเสธเขียนลง Firestore **เฉพาะช่อง `status`** ช่องอื่นไม่แตะ

### 3.5 `seed.html` — ใส่ข้อมูลตัวอย่าง
เครื่องมือตั้งระบบครั้งแรก ไม่ใช่หน้าของผู้ใช้ · ชื่อสมมติทั้งหมด
เขียน `products` 3 รายการ · `users` 5 คน (buyer 3 · rep 2) · `requests` 5 ใบ (ครบทั้ง 3 สถานะ) · `requests/r002/replies` 1 รายการ
คำขอจาก seed ไม่มีช่อง `urgency` — โค้ดฝั่งอ่านรองรับไว้แล้ว แสดงเป็น `-`

### แผนผังการเดิน

```
login.html ──(มีโปรไฟล์)──> index.html ──> detail.html
    │                           │              │
    └─(ไม่มีโปรไฟล์)─> complete  └─(buyer)─> new.html ──> index.html
```
ทุกหน้าที่ต้องล็อกอินเรียก `requireAuth()` — ไม่ได้ล็อกอินเด้งไป `login.html?next=…`

---

## 4. โครงสร้างข้อมูล

Firestore · ชื่อคอลเลกชันตาม `SCOPE.md`

```
users                     คนที่ใช้ระบบ · document id = uid จาก Firebase Auth
products                  โฟลเดอร์ประเภท — รายการยาที่เลือกได้
requests                  โฟลเดอร์หลัก — คำขอหายา
  ├── replies             โฟลเดอร์ย่อย — คำตอบของผู้แทนในคำขอใบนั้น
  └── aiLog               โฟลเดอร์ย่อย — บันทึกทุกครั้งที่ AI ระดับ 2 ทำงาน
```

### `users/{uid}`
| ช่อง | ชนิด | หมายเหตุ |
|---|---|---|
| `email` | string | |
| `name` | string | ชื่อร้าน / คลินิก / ชื่อผู้แทน |
| `role` | `"buyer"` \| `"rep"` | แหล่งความจริงเดียวของสิทธิ์ |
| `channel` | `"OTC"` \| `"MC"` | hard gate — ล็อกตั้งแต่สมัคร |
| `area` | string | เขต / อำเภอ |

### `products/{id}`
| ช่อง | ชนิด | หมายเหตุ |
|---|---|---|
| `name` | string | ชื่อที่แสดงใน dropdown และจดซ้ำลง `requests.productName` |
| `company` | string | บริษัทผู้ผลิต — AI ระดับ 2 อ่านไปใช้ |
| `channel` | `"OTC"` \| `"MC"` | กรอง dropdown ใน `new.html` |

### `requests/{id}` — โฟลเดอร์หลัก
| ช่อง | ชนิด | ใครเขียน |
|---|---|---|
| `buyerId` | string (uid) | `new.html` — ช่องบอกว่าคำขอนี้เป็นของใคร |
| `buyerName` | string | `new.html` · **จดซ้ำตั้งใจ** |
| `buyerChannel` | `"OTC"` \| `"MC"` | `new.html` |
| `buyerArea` | string | `new.html` — สิ่งเดียวที่ผู้แทนเห็นก่อนกดรับ |
| `productId` | string | `new.html` |
| `productName` | string | `new.html` · **จดซ้ำตั้งใจ** |
| `qty` | number | `new.html` |
| `unit` | string | แผง · กล่อง · ขวด · ซอง · ลัง · ชิ้น |
| `note` | string | ช่องข้อความยาวที่ร้านพิมพ์เอง — AI ทั้ง 2 ระดับอ่านช่องนี้ |
| `urgency` | string | ร้านเลือกเอง (AI ระดับ 1 เสนอค่าให้ได้) — `URGENCY` ใน `app/db.js` |
| `status` | string | `new.html` ตั้งต้น · หลังจากนั้น **rep เท่านั้น** |
| `createdAt` | timestamp | `serverTimestamp()` |
| `aiSuggestion` `aiReason` `aiUpdatedAt` | string · string · timestamp | `detail.html` (AI ระดับ 2) — **ห้ามแตะ `status`** |

🔴 **Document ID ไม่ใช่ field** — ตอนอ่านต้องประกอบ `{ id: d.id, ...d.data() }` กลับเสมอ

### `requests/{id}/replies/{id}` — โฟลเดอร์ย่อย
`repId` · `repName` · `message` · `contactPhone` · `createdAt`
กำหนดสคีมาไว้แล้ว และ AI ระดับ 2 อ่านมาใช้ **แต่ยังไม่มีหน้าจอให้เขียนหรืออ่าน** (ดูข้อ 7.3)

### `requests/{id}/aiLog/{id}` — โฟลเดอร์ย่อย
`input` · `output` · `steps` (array บอกว่า AI ไปอ่านอะไรมา) · `model` · `byUid` · `createdAt`

### สถานะ — มี 3 ค่า อ่านจาก `STATUS` ใน `app/db.js` ที่เดียว

```
รอผู้แทนรับ  ──(ผู้แทนกดรับ)──>    รับแล้ว     (ชื่อร้านเปิดเผย)
รอผู้แทนรับ  ──(ผู้แทนกดปฏิเสธ)──>  ปฏิเสธ
```
เดินทางเดียว ไม่มีย้อนกลับ ไม่มีแก้ซ้ำ · ร้านสร้างคำขอได้ `รอผู้แทนรับ` เสมอ

---

## 5. ผู้ช่วย AI

เรียก OpenRouter ผ่าน `app/ai.js` ที่เดียว · timeout 15 วินาที · คีย์อ่านจาก `app/ai-config.js` ที่ `.gitignore` กันไว้
**ไม่ใส่คีย์ ระบบยังใช้งานได้ครบทุกอย่าง** — ปุ่ม AI แค่ถูกปิด

| ระดับ | หน้า | ใครกด | ทำอะไร |
|---|---|---|---|
| 1 | `new.html` | buyer | ถามครั้งเดียว — อ่าน `note` แล้วเสนอรายการยา (ต้องตรงกับรายการที่มีจริงเท่านั้น ไม่ตรงก็ไม่แตะค่าเดิม) + ความเร่งด่วน |
| 2 | `detail.html` | rep | agentic 4 ขั้น — อ่านคำขอใบนี้ → `products/{productId}` → `replies` → คำขอรายการเดียวกันที่ยังรออยู่ในช่องทางเดียวกัน → สรุปให้อ่าน → เขียนลง `aiSuggestion`/`aiReason` → จดลง `aiLog` |

กติกาที่ prompt บังคับไว้ (ตรงกับข้อห้ามของโปรเจกต์): ห้ามตัดสินใจแทน · ห้ามให้คะแนน เปอร์เซ็นต์ ดาว หรือจัดอันดับ · ห้ามพูดถึงราคา/ค่าส่ง/การชำระเงิน · ห้ามเดาตารางเดินทางของผู้แทน · **ไม่ส่งชื่อร้านเข้า prompt** ส่งแค่เขต
ทั้งสองระดับมีสัญญาณกำลังทำงาน กดซ้ำไม่ได้ และมีป้าย "ข้อเสนอจาก AI — โปรดตรวจสอบก่อนยืนยัน"

---

## 6. เทคนิคและการขึ้นระบบ

HTML + CSS + JS ล้วน · ไม่มี npm ไม่มี build · Firebase SDK จาก CDN gstatic **ตรึง 12.18.0**
Firestore + Firebase Auth (Email/Password) + Firebase Hosting · ธีม dark/light ตามเครื่อง ผ่านตัวแปร CSS ใน `app/style.css`

รันในเครื่อง: `python3 -m http.server 8000` (เปิดด้วย `file://` ไม่ได้ เพราะใช้ ES module)
ขึ้นออนไลน์: `npx firebase-tools deploy --only hosting,firestore:rules` — **ห้าม deploy เว็บโดยไม่ deploy กฎไปพร้อมกัน**

🔴 **Security rules ตอนนี้ยังหลวม** — มีกฎเดียวคือ "ต้องล็อกอินก่อน" ใครสมัครเข้ามาก็ยิง query ตรงอ่านของคนอื่นได้
ACL ทั้งตารางตอนนี้บังคับอยู่ที่ **ฝั่งหน้าเว็บเท่านั้น** → ห้ามใส่ข้อมูลจริงของบุคคลอื่นจนกว่าจะย้ายลงเป็นกฎเสร็จ

---

## 7. ไม่ทำใน Module นี้

### 7.1 ตัดออกตั้งแต่กำหนดขอบเขต (อยู่ใน Backlog Sprint 2)
- **แคตตาล็อกยาเต็ม** — Module นี้มี `products` แค่พอให้เลือก ไม่มีหน้าจัดการ เพิ่ม/แก้/ลบรายการยา
- **OCR ใบส่งของ**
- **ตรวจใบอนุญาต** ของร้านหรือผู้แทน
- **แดชบอร์ด / รายงาน / สถิติ**
- **ระบบ VIP**
- **สถานะที่ 4 "ตกลงราคา"** — ของจริงมี แต่ตัดไว้ให้เหลือ 3 สถานะตามคู่มือ
- **หน้าแคตตาล็อกของผู้แทน** — เป็นหน้าที่ 4 ที่ `SCOPE.md` เขียนไว้ว่าอาจมี สุดท้ายไม่ได้ทำ

### 7.2 ไม่ทำเพราะเป็นข้อห้ามของโปรเจกต์ (ไม่ใช่เลื่อนไป Sprint หน้า)
- **ไม่มีคะแนนแมตช์เป็นตัวเลข** — ไม่มีดาว ไม่มีเปอร์เซ็นต์ ไม่จัดอันดับผู้แทน แสดงได้แค่ข้อเท็จจริง
- **ไม่มีการซื้อขายในแอป** — ไม่มีราคา ราคารวม ค่าส่ง ยอดขั้นต่ำ ปุ่มชำระเงิน ติดตามพัสดุ ระบบจบที่ตกลงกันได้
- **ไม่อ้างรอบวิ่งสายของผู้แทน** — ระบบไม่รู้ตารางงานจริง พูดได้แค่ "อยู่ใกล้กัน"
- **ไม่ใส่ข้อมูลจริงของบุคคลอื่น** — ข้อมูลตัวอย่างเป็นชื่อสมมติทั้งหมด ไม่ใช้ชุด สปสช. ที่มีอยู่จริง
- **ไม่ให้ผู้ใช้เลือก OTC/MC เองหลังสมัคร** — เป็น hard gate ที่โปรไฟล์ ไม่ใช่ช่องติ๊ก

### 7.3 มีสคีมาแล้วแต่ยังไม่มีหน้าจอ
- **`replies`** — นิยามช่องครบ (`repId` · `repName` · `message` · `contactPhone` · `createdAt`) · `seed.html` ใส่ตัวอย่างไว้ 1 รายการ · AI ระดับ 2 อ่านมาใช้แล้ว
  **แต่ไม่มี UI ให้ผู้แทนเขียนคำตอบ และไม่มีที่แสดงคำตอบใน `detail.html`** — ข้อมูลที่เกิดจากการใช้งานจริงจึงเป็นศูนย์ และช่องทางส่งเบอร์ติดต่อกลับให้ร้านยังไม่เปิด

### 7.4 ที่ยังไม่ได้ทำฝั่งเทคนิค
- **Security rules รายคอลเลกชัน** — ยังเป็นกฎเดียว "ต้องล็อกอิน" (งานสัปดาห์ที่ 8 ที่ยังค้าง)
- **เรียก AI ผ่านตัวกลางฝั่งเซิร์ฟเวอร์** — ตอนนี้คีย์อยู่ฝั่งหน้าเว็บ เปิด F12 ก็เห็น ยอมรับได้เฉพาะในหลักสูตรเพราะคีย์มีวงเงินจำกัด
- **เทสอัตโนมัติที่ต้องล็อกอิน** — Playwright มีเทสเดียว (ยังไม่ล็อกอินต้องเด้งไปหน้าเข้าสู่ระบบ) ส่วนกดปุ่ม AI และเปลี่ยนสถานะยังไม่ได้ทำ
- **แก้ไขคำขอหลังส่ง** — ร้านทำได้แค่ลบ ไม่มีหน้าแก้
- **แจ้งเตือน** — ไม่มี push ไม่มีอีเมล ผู้แทนต้องเปิดหน้ารายการเอง
- **ค้นหา / กรอง / แบ่งหน้าในรายการคำขอ** — อ่านมาทั้งหมดแล้วเรียงฝั่งหน้าเว็บ ยังไม่คิดเรื่องจำนวนเยอะ
