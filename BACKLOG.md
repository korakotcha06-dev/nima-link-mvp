# BACKLOG — ของที่ยังไม่ทำ และเหตุผล

ส่งต่อให้ Module 3 · ปรับปรุงล่าสุด 23 ก.ย. 2569

เอกสารนี้ไม่ใช่รายการบั๊ก ทุกข้อในนี้คือเรื่องที่ **รู้แล้ว เข้าใจสาเหตุแล้ว และตัดสินใจแล้วว่าจะไม่ทำใน Module 2**
แต่ละข้อเขียน 3 ส่วนเสมอ — **คืออะไร · ทำไมถึงเป็นแบบนั้น · ทางแก้จริงต้องทำอะไร**

อ่านเอกสารนี้ก่อนคิดจะ "แก้" อะไรในระบบ ถ้าอาการที่เจอตรงกับข้อใดข้อหนึ่งในนี้ แปลว่าระบบทำงานตามที่ตั้งใจแล้ว

---

## A. ความเสี่ยงที่รับไว้แล้ว — ต้องจัดการใน Module 3

### A1. คีย์ AI อยู่ฝั่งหน้าเว็บ ใครเปิด DevTools ก็เห็น

**คืออะไร** — `app/ai.js` เรียก OpenRouter จากเบราว์เซอร์โดยตรง คีย์อยู่ใน `app/ai-config.js`
ซึ่ง `.gitignore` กันไม่ให้ขึ้น repo แต่ไฟล์นี้ถูกส่งไปกับหน้าเว็บที่เผยแพร่สาธารณะ

**ทำไม** — ทางแก้ที่ถูกต้องคือย้ายไปเรียกจากฝั่งเซิร์ฟเวอร์ ซึ่งต้องใช้ Firebase Functions = เปิด billing แบบ Blaze
บวกกับมีขั้นตอน build และ npm ทั้งสองอย่างขัดกับสแตกที่ประกาศไว้ว่า **ไม่มี build ไม่มีเซิร์ฟเวอร์ของเราเอง**
ทัชตัดสินใจไว้เมื่อ 20 ก.ย. 2569 ว่ายอมรับข้อแลกนี้ในระดับงานเรียน

**ทางแก้จริง** — ย้ายการเรียก AI ไปอยู่หลังเซิร์ฟเวอร์ที่ผู้ใช้แตะไม่ได้ (Cloud Function หรือ backend เล็ก ๆ)
ระหว่างนี้ **ใช้ได้เฉพาะคีย์ที่มีวงเงินจำกัด ห้ามใช้คีย์ส่วนตัว และเพิกถอนคีย์ทิ้งเมื่อจบโครงงาน**

### A2. บัญชีทดสอบ `e2e-*` ค้างอยู่ในระบบจริง

**คืออะไร** — ชุดเทสสมัครบัญชีทดสอบ 5 ใบไว้บน Firebase Auth จริง และมีเอกสาร `users/{uid}` ของตัวเองอยู่

**ทำไม** — โปรเจกต์นี้ไม่มี emulator เทสจึงต้องรันกับของจริง และกฎตั้งใจห้ามลบเอกสาร `users`
(`allow delete: if false`) เพราะระบบจริงไม่ควรมีใครลบโปรไฟล์คนอื่นได้ ส่วนบัญชี Auth ต้องลบจาก Console

**ทางแก้จริง** — ถ้าจะทำให้สะอาดต้องมี Firebase emulator หรือโปรเจกต์ Firebase แยกสำหรับเทสโดยเฉพาะ
ระหว่างนี้ลบด้วยมือได้ที่ Firebase Console > Authentication และ Firestore > `users`

---

## B. มีสคีมาและกฎแล้ว แต่ยังไม่มีเทสอัตโนมัติ

### B1. คำตอบของผู้แทนและการแลกเบอร์ติดต่อ (`replies`)

**คืออะไร** — หน้าจอใช้งานได้จริงแล้วใน `detail.html` และกฎล็อกไว้ครบ (เฉพาะผู้แทนที่กดรับใบนั้นจึงเขียนได้
ร้านเจ้าของกับผู้แทนคนนั้นอ่านได้) แต่ยังไม่มีเทสอัตโนมัติที่เดินครบเส้น กดรับ แล้วส่งคำตอบพร้อมเบอร์

**ทำไม** — เส้นนี้ต้องสลับบัญชี 2 ใบและผ่านด่านกดรับก่อน ซึ่งเป็นเทสที่ยาวที่สุดในชุดอยู่แล้ว
สัปดาห์ที่ 9 เลือกใช้เวลาที่มีไปกับเทสความปลอดภัยที่โจทย์บังคับก่อน

**ทางแก้จริง** — เพิ่มไฟล์เทสที่ต่อจากเทส "กดรับ" แล้วยืนยันว่าผู้แทนคนอื่นในช่องทางเดียวกันอ่าน `contactPhone` ไม่ได้

### B2. หน้าแก้ไขคำขอ (`edit.html`)

**คืออะไร** — กฎเขียนเป็น allowlist แล้ว (แก้ได้เฉพาะ `productId` `productName` `qty` `unit` `note` `urgency`)
แต่ยังไม่มีเทสยืนยันว่า `status` และ `buyerId` ขยับไม่ได้จริงผ่านหน้านี้

**ทำไม** — เพิ่งทำเสร็จในสัปดาห์ที่ 9 พร้อมกับงานอื่น

**ทางแก้จริง** — เพิ่มเทสยิงคำสั่งตรงที่พยายามแก้ช่องนอก allowlist แล้วต้องได้ `permission-denied`

### B3. ผู้ช่วย AI ทั้ง 2 ระดับ

**คืออะไร** — ไม่มีเทสอัตโนมัติที่กดปุ่ม AI จริง

**ทำไม** — คำตอบของโมเดลไม่คงที่พอจะเขียน assertion ตรง ๆ และการรันทุกครั้งกินวงเงินคีย์

**ทางแก้จริง** — ถ้าจะทดสอบให้จริงจังต้อง mock ชั้นเรียก AI แล้วทดสอบ "พฤติกรรมของหน้าเว็บเมื่อ AI ตอบแบบนั้น"
ไม่ใช่ทดสอบตัวโมเดล — สิ่งที่ทดสอบไปแล้วคือระบบต้องใช้งานได้ครบเมื่อไม่มีคีย์

---

## C. ตัดออกตั้งแต่กำหนดขอบเขต — อยู่ใน Sprint 2 ของโครงงานจริง

ทั้งหมดนี้เป็นของโครงงาน NIMA Link เต็ม ไม่ใช่งานค้างของ Module 2

- **แคตตาล็อกยาเต็ม** — ตอนนี้ `products` มีแค่พอให้เลือก ไม่มีหน้าจัดการเพิ่ม/แก้/ลบ (ใส่ผ่าน Console)
- **OCR ใบส่งของ** — อ่านใบส่งของแล้วเติมรายการยาอัตโนมัติ
- **ตรวจใบอนุญาต** ของร้านและของผู้แทน — เป็นเรื่องกฎหมาย ต้องออกแบบร่วมกับคนที่รู้จริง
- **แดชบอร์ด รายงาน สถิติ** — ต้องระวังไม่ให้กลายเป็นการจัดอันดับผู้แทน ซึ่งเป็นข้อห้ามข้อ 1 ของโปรเจกต์
- **ระบบ VIP**
- **สถานะที่ 4 "ตกลงราคา"** — ของจริงมี แต่ Module 2 คุมให้เหลือ 3 สถานะตามคู่มือ
- **แบ่งหน้า (pagination)** — ตอนนี้อ่านคำขอมาทั้งหมดแล้วกรองฝั่งหน้าเว็บ ยังไม่คิดเรื่องจำนวนหลักพันใบ
- **ระบบแจ้งเตือน** (push / อีเมล / LINE) — ต้องมีฝั่งเซิร์ฟเวอร์ ตัดไปพร้อมข้อ A1

---

## D. ไม่ทำเพราะเป็นข้อห้ามของโปรเจกต์ — ไม่ใช่งานค้าง

ข้อพวกนี้จะไม่ถูกทำ ไม่ว่าจะมีเวลาเท่าไหร่ ใครเสนอให้ทำคือเข้าใจโจทย์ผิด

1. **ไม่มีคะแนนแมตช์เป็นตัวเลข** — ไม่มีดาว ไม่มีเปอร์เซ็นต์ ไม่จัดอันดับผู้แทน แสดงได้แค่ข้อเท็จจริง
2. **ไม่มีการซื้อขายในแอป** — ไม่มีราคา ค่าส่ง ยอดขั้นต่ำ ปุ่มชำระเงิน ติดตามพัสดุ ระบบจบที่ตกลงกันได้
3. **ไม่อ้างรอบวิ่งสายของผู้แทน** — ระบบไม่รู้ตารางงานจริงของเขา พูดได้แค่ว่าอยู่ใกล้กัน
4. **ไม่ให้ผู้ใช้เปลี่ยน OTC/MC เองหลังสมัคร** — เป็น hard gate ที่โปรไฟล์ ไม่ใช่ช่องติ๊ก
5. **ไม่ใส่ข้อมูลจริงของบุคคลอื่น** — repo นี้เปิดสาธารณะในฐานะงานเรียน ข้อมูลตัวอย่างเป็นชื่อสมมติทั้งหมด
6. **ไม่มีบทบาทที่ 3 และไม่มีหน้าผู้ดูแลระบบ** — เพิ่มบทบาทแปลว่าต้องรื้อกฎทั้งไฟล์
