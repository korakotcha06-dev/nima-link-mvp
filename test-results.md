# ผลการทดสอบ NIMA Link

รันเมื่อ **วันพุธที่ 23 กันยายน พ.ศ. 2569 เวลา 08:55 น.** (เวลาไทย) ใช้เวลารวม 56 วินาที

| ชุดทดสอบ | ทั้งหมด | ผ่าน | ไม่ผ่าน |
|---|---:|---:|---:|
| `tests/acl-ui.spec.js` — เดินผ่านหน้าเว็บจริง | 9 | 9 | 0 |
| `tests/acl-db.spec.js` — ยิงคำสั่งตรงเข้าชั้นข้อมูล | 8 | 8 | 0 |
| `tests/03-form-validation.spec.js` — ฟอร์มกรอกไม่ครบ | 3 | 3 | 0 |
| **รวม** | **20** | **20** | **0** |

ผ่านทั้งหมด ไม่มีข้อที่ไม่ผ่านในรอบนี้

เทสทั้งชุดยิงกับ **เว็บจริง** <https://nima-link-mvp.web.app> และ **Firestore จริง** ของโปรเจกต์
`nima-link-mvp` — โปรเจกต์นี้ไม่มี emulator ตามที่ตัดสินใจไว้ ทุกข้อจึงเป็นการทดสอบระบบที่ผู้ใช้เปิดเจอจริง

วิธีรันซ้ำอยู่ใน [`tests/README.md`](tests/README.md) โดยย่อคือ `npm install` ครั้งเดียว แล้ว `npm test`

---

## 1. เทส 5 ข้อที่การบ้านบังคับ อยู่ตรงไหนบ้าง

| # | โจทย์บังคับให้ทดสอบ | เทสที่รับผิดชอบ | ผล |
|---|---|---|---|
| 1 | เส้นทางหลักของระบบ | `acl-ui` — buyer สร้างคำขอได้ผ่านฟอร์มจริง แล้วได้สถานะ รอผู้แทนรับ | ผ่าน |
| 2 | ปุ่มที่เปลี่ยนสถานะ | `acl-ui` — rep กดรับแล้วชื่อร้านเปิดเผย และกดซ้ำไม่ได้ | ผ่าน |
| 3 | ฟอร์มกรอกไม่ครบต้องไม่บันทึก | `03-form-validation` ทั้ง 3 ข้อ | ผ่าน |
| 4 | คนที่ยังไม่ล็อกอินเข้าถึงข้อมูลไม่ได้ | `acl-ui` 3 ข้อแรก | ผ่าน |
| 5 | บัญชีที่สองแตะข้อมูลของบัญชีแรกไม่ได้ | `acl-ui` — ร้านอื่นเปิดคำขอไม่ได้ · `acl-db` — ยิงคำสั่งตรงแล้วถูกปฏิเสธ 7 ข้อ | ผ่าน |

ข้อ 4 และ 5 ถูกทดสอบสองชั้นโดยตั้งใจ — ชั้นหน้าจอ (เห็นอะไรบ้าง) และชั้นฐานข้อมูล (ยิงคำสั่งตรงได้ไหม)
**การซ่อนปุ่มไม่ใช่ความปลอดภัย** ใครก็ตามที่เปิด DevTools แล้วสั่ง Firestore เองก็ข้ามหน้าเว็บไปได้ทั้งหน้า
`tests/acl-db.spec.js` จึงข้าม UI ไปยิงคำสั่งตรงทุกข้อ เพื่อพิสูจน์ว่า `firestore.rules` กันจริง ไม่ใช่กันแค่บนจอ

---

## 2. ผลทีละข้อ

### 2.1 เดินผ่านหน้าเว็บจริง — `tests/acl-ui.spec.js`

| # | ทดสอบอะไร | ทำไมต้องมีข้อนี้ | ผล | เวลา |
|---:|---|---|---|---:|
| 1 | ยังไม่ล็อกอิน เปิดหน้าสร้างคำขอ ต้องเด้งไปหน้าเข้าสู่ระบบ | หน้าเขียนข้อมูลต้องไม่เปิดให้คนนอก | ผ่าน | 1.0 วิ |
| 2 | ยังไม่ล็อกอิน เปิดหน้ารายการคำขอ ต้องเด้งไปหน้าเข้าสู่ระบบ | หน้ารายการคือที่ที่ข้อมูลของทุกร้านมารวมกัน | ผ่าน | 1.0 วิ |
| 3 | ยังไม่ล็อกอิน เปิดหน้ารายละเอียด ต้องเด้งไป และจำที่เดิมไว้ด้วย `next=` | กันข้อมูลรั่ว โดยที่ยังพากลับมาที่เดิมได้หลังล็อกอิน | ผ่าน | 0.8 วิ |
| 4 | ผู้แทนเปิดหน้าสร้างคำขอ ต้องเห็นข้อความปฏิเสธและฟอร์มไม่โผล่ | คำขอเป็นคำพูดของร้าน ผู้แทนพูดแทนไม่ได้ (spec 8.3) | ผ่าน | 4.2 วิ |
| 5 | ร้านสร้างคำขอผ่านฟอร์มจริง แล้วได้สถานะ รอผู้แทนรับ | นี่คือเส้นทางหลักของระบบทั้งระบบ | ผ่าน | 7.0 วิ |
| 6 | ร้านเปิดคำขอของตัวเอง ต้องไม่เห็นปุ่มรับ/ปฏิเสธ | ร้านอนุมัติของตัวเองไม่ได้ ไม่งั้นตัวเลขจับคู่ไม่มีความหมาย | ผ่าน | 4.5 วิ |
| 7 | ร้านอื่นเปิดคำขอใบนี้ไม่ได้ ต้องเห็นข้อความปฏิเสธ | คำขอบอกว่าร้านไหนของขาด เป็นข้อมูลการค้า | ผ่าน | 3.9 วิ |
| 8 | ผู้แทนต่างช่องทาง (MC เปิดของ OTC) ต้องเปิดไม่ได้ | OTC/MC เป็น hard gate คนละกฎหมายควบคุม | ผ่าน | 7.7 วิ |
| 9 | ผู้แทนช่องทางเดียวกันเห็นแค่เขตจนกว่าจะกดรับ · กดรับแล้วเห็นชื่อร้าน · กดซ้ำไม่ได้ | เส้นแดง PDPA + สถานะเปลี่ยนได้ครั้งเดียว รวมไว้ในเส้นเดียว | ผ่าน | 4.6 วิ |
| 10 | ร้านลบคำขอของตัวเองได้ และต้องมีกล่องยืนยันก่อนลบทุกครั้ง | ลบแล้วกู้ไม่ได้ จึงต้องถามก่อนเสมอ | ผ่าน | 20.7 วิ |

ข้อ 9 กับ 10 เป็นเทสที่ยาวที่สุดเพราะต้องสลับบัญชีจริงหลายรอบในเทสเดียว

### 2.2 ยิงคำสั่งตรงเข้าชั้นข้อมูล — `tests/acl-db.spec.js`

ทุกข้อในหัวข้อนี้ล็อกอินผ่านหน้าเว็บจริงก่อน แล้ว **ข้าม UI** ไปเรียก Firestore SDK ตรงจากหน้านั้น
คำตอบที่ต้องการคือ `permission-denied` ทุกข้อ ถ้าข้อใดข้อหนึ่งผ่านไปได้ แปลว่าข้อมูลรั่วจริง ไม่ใช่แค่ UI ไม่สวย

| # | ทดสอบอะไร | ผล | เวลา |
|---:|---|---|---:|
| 1 | ร้านยิงอ่านคำขอของร้านอื่นตรง ๆ | ถูกปฏิเสธ — ผ่าน | 10.1 วิ |
| 2 | ผู้แทนยิงอ่านคำขอข้ามช่องทาง (MC อ่านของ OTC) | ถูกปฏิเสธ — ผ่าน | 10.0 วิ |
| 3 | ร้านยิงเปลี่ยนสถานะคำขอของตัวเอง | ถูกปฏิเสธ — ผ่าน | 4.1 วิ |
| 4 | ผู้แทนยิงแก้ `qty` ซึ่งไม่ใช่ช่อง `status` | ถูกปฏิเสธ — ผ่าน | 9.6 วิ |
| 5 | ผู้แทนยิงสร้างคำขอเอง | ถูกปฏิเสธ — ผ่าน | 4.2 วิ |
| 6 | ผู้แทนยิงลบคำขอที่ไม่ใช่ของตัวเอง | ถูกปฏิเสธ — ผ่าน | 9.8 วิ |
| 7 | ผู้ใช้ยิงแก้ `role` ของตัวเองใน `users/{uid}` เป็น `rep` | ถูกปฏิเสธ — ผ่าน | 3.8 วิ |

ข้อ 7 คือข้อที่กันไม่ให้ข้ออื่นทั้งหมดพัง — ถ้าเปลี่ยน role ตัวเองได้ ใครก็ยกตัวเองเป็นผู้แทนแล้วเดินผ่านกฎที่เหลือได้หมด

### 2.3 ฟอร์มกรอกไม่ครบ — `tests/03-form-validation.spec.js`

ทุกข้อตรวจ 3 ชั้น: ยังอยู่หน้าเดิมหรือไม่ · เบราว์เซอร์บอกผู้ใช้หรือไม่ · **มีเอกสารเกิดขึ้นในฐานข้อมูลหรือไม่**
ชั้นที่สามสำคัญที่สุด เพราะดูแค่หน้าจอจะไม่มีทางรู้ว่ามีข้อมูลพัง ๆ ไหลเข้าฐานไปแล้วหรือยัง

| # | ทดสอบอะไร | ผล | เวลา |
|---:|---|---|---:|
| 1 | เว้นช่องจำนวนไว้แล้วกดส่ง | ไม่บันทึก มีข้อความบอก ไม่มีเอกสารเกิด — ผ่าน | 4.2 วิ |
| 2 | ใส่จำนวนติดลบ | ไม่บันทึก ไม่มีเอกสารเกิด — ผ่าน | 4.3 วิ |
| 3 | ใส่จำนวนเป็นทศนิยม (2.5) | ไม่บันทึก ไม่มีเอกสารเกิด — ผ่าน | 5.1 วิ |

---

## 3. ข้อที่ไม่ผ่าน

ไม่มี ทุกข้อผ่านหมดในรอบนี้

---

## 4. สิ่งที่ยังทดสอบอัตโนมัติไม่ได้

เขียนไว้ตรง ๆ เพราะรายงานที่ดูสะอาดเกินจริงมีค่าน้อยกว่ารายงานที่บอกว่าตัวเองมองไม่เห็นอะไร

- **ผู้ช่วย AI ทั้ง 2 ระดับ** — ยังไม่มีเทสที่กดปุ่ม AI จริง เพราะคำตอบของโมเดลไม่คงที่พอจะยืนยันด้วย assertion
  และการรันทุกครั้งกินวงเงินคีย์ของหลักสูตร · สิ่งที่ทดสอบแล้วคือระบบต้องใช้งานได้ตามปกติเมื่อไม่มีคีย์
- **คำตอบของผู้แทน (`replies`) และการแลกเบอร์ติดต่อ** — กฎฝั่งฐานข้อมูลเขียนครบแล้ว แต่ยังไม่มีเทสอัตโนมัติ
  ที่เดินครบเส้น กดรับ แล้วส่งคำตอบพร้อมเบอร์
- **หน้าแก้ไขคำขอ (`edit.html`)** — ยังไม่มีเทสยืนยันว่าแก้ได้เฉพาะช่องที่อนุญาต
- **ธีม dark / light และการแสดงผลบนจอเล็ก** — ต้องใช้สายตาคนดู เทสอัตโนมัติชุดนี้ไม่ได้ตรวจ
- **จำนวนข้อมูลเยอะ ๆ** — ระบบอ่านคำขอมาทั้งหมดแล้วกรองฝั่งหน้าเว็บ ยังไม่ได้ทดสอบว่าที่หลักพันใบจะเป็นอย่างไร

ของที่ยังไม่ทำพร้อมเหตุผล อยู่ใน [`BACKLOG.md`](BACKLOG.md)

---

## 5. ข้อมูลที่เทสสร้างทิ้งไว้

เทสรันกับฐานข้อมูลจริง จึงเก็บกวาดของตัวเองท้ายเทสทุกครั้ง — คำขอที่สร้างถูกลบผ่านหน้าเว็บจริง
สิ่งที่ยังค้างในระบบคือ **บัญชีทดสอบ 5 ใบที่ขึ้นต้นด้วย `e2e-`** ซึ่งลบจากฝั่งหน้าเว็บไม่ได้
(กฎห้ามลบเอกสาร `users` และ Firebase Auth ต้องลบจาก Console) — บันทึกไว้ใน `BACKLOG.md` แล้ว
